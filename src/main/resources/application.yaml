server:
  port: 8081

spring:
  application:
    name: auth-service
  secret: stayease-super-secret-key-for-jwt-authentication-2026-secure-key

  mail:
    host: smtp.gmail.com
    port: 587
    username: sairam.paidipati32@gmail.com
    password: oplj qscp xdst nwmk
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  datasource:
    url: jdbc:mysql://localhost:3306/stayease_auth_db
    username: root
    password: 2000030757
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

services:
  user-service:
    url: http://localhost:8082
  owner-service:
    url: http://localhost:8083

logging:
  level:
    root: INFO
    com.stayease.auth_service: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: INFO
    feign: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
